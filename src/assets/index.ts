const Background = {
  LogIn: require('./background.png'),
  Category: require('./background2.png'),
};

const Icon = {
  Eye: require('./eye.png'),
  Arrow: require('./arrow.png'),
  ArrowRight: require('./arrow-right.png'),
  CheckedBox: require('./checked-box.png'),
  UncheckedBox: require('./unchecked-box.png'),
};

const Images = {
  Background,
  Icon,
};

export default Images;
